// Bu işlev, "Add to My Reservations" butonuna tıklandığında çağrılacaktır.
function addToMyReservations(hotelName, location) {
    // Bu kısımda, gerçek bir rezervasyon ekleme işlemi yapılabilir.
    // Örneğin, bir API'ye istek gönderilebilir veya tarayıcı yerel depolama kullanılabilir.
  
    // Bu örnek uygulamada, sadece bir bildirim gösteriyoruz.
    alert(`Hotel "${hotelName}" in ${location} added to My Reservations.`);
  }